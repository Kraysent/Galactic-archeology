{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Creation config YAML schema",
  "description": "YAML schema for creation configuration file of OMTool",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "output_file",
    "objects"
  ],
  "properties": {
    "output_file": {
      "type": "string",
      "description": "Path to the file where the output model would be saved."
    },
    "overwrite": {
      "type": "boolean",
      "description": "Should the program overwrite model file if it already exists or not.",
      "default": false
    },
    "logging": {
      "type": "object",
      "description": "Object that stores configuration for logger.",
      "additionalProperties": false,
      "properties": {
        "filename": {
          "type": "string",
          "description": "Filename where JSON logs would be stored."
        },
        "datefmt": {
          "type": "string",
          "description": "Format of the date provided to logging module."
        }
      }
    },
    "objects": {
      "type": "array",
      "description": "List of objects that would be added to model.",
      "items": {
        "type": "object",
        "additionalItems": true,
        "required": [
          "type"
        ],
        "properties": {
          "type": {
            "type": "string",
            "description": "Type of object.",
            "enum": [
              "body",
              "csv",
              "plummer_sphere"
            ]
          },
          "args": {
            "type": "object",
            "description": "Parameters to the constructor of the model."
          },
          "position": {
            "type": "array",
            "description": "Initial position of the system. Should be denoted with !q tag."
          },
          "velocity": {
            "type": "array",
            "description": "Initial velocity of the system. Should be denoted with !q tag."
          }
        }
      }
    }
  }
}